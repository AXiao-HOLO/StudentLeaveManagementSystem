<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.student.mapper.CourseMapper">

    <resultMap id="BaseResultMap" type="com.student.domain.Course">
            <id property="id" column="id" />
            <result property="courseName" column="course_name" />
            <result property="courseCode" column="course_code" />
            <result property="credit" column="credit" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="Base_Column_List">
        id,course_name,course_code,credit,create_time,update_time
    </sql>
    <select id="list" resultType="com.student.domain.Course">
        -- 查询所有的课程信息，支持模糊查询所有信息
        select * from course
        <where>
            <if test="courseName != null and courseName != ''">
                and course_name like concat('%',#{courseName},'%')
            </if>
            <if test="courseCode != null and courseCode != ''">
                and course_code like concat('%',#{courseCode},'%')
            </if>
            <if test="credit != null">
                and credit = #{credit}
            </if>
        </where>

    </select>
</mapper>
